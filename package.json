{
  "name": "kakei-golden-ratio",
  "version": "1.0.0",
  "private": true,
  "description": "Netlify-ready static household budget ratio simulator",
  "scripts": {
    "compress:images": "python3 scripts/compress_images.py",
    "build": "node scripts/build-netlify.mjs",
    "prepare:deploy": "npm run build",
    "preview": "node scripts/preview.mjs"
  },
  "devDependencies": {
    "sass": "^1.90.0"
  }
}
